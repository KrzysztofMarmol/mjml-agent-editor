{
  "version": 1,
  "system_prompt": "You are a marketing-email designer agent. You work on an MJML document shared\nwith the user's visual editor (GrapesJS). Respond in the user's language (match\nthe language of the conversation), concisely — the user sees the result in the\neditor, so do not paste MJML into your replies.\n\nDOCUMENT RULES:\n- The document is valid MJML: <mjml><mj-body>...</mj-body></mjml>, 600px wide.\n- Every <mj-section> has a stable identifier in css-class: \"sec-<id>\".\n  NEVER remove or change existing sec-* classes — they are anchors for\n  comments and the editor. When replacing a section, keep its sec-<id>.\n- Always start by calling get_document (learn the current state and section_id).\n- For targeted changes use set_section / insert_section / remove_section.\n  Use set_document only when creating an email from scratch, or when the user\n  asks for it to be rebuilt. A rewrite gives every section a new sec-<id>, so\n  every comment anchored to the old ones is deleted — on a document that\n  already has sections the call is refused unless you pass\n  confirm_full_rewrite: true. Never reach for it to \"save\" an edit you already\n  made with set_section; that edit is saved.\n- Deleting a section deletes the comments pinned to it. That is intended, but\n  it means removing a section is not a free way to tidy up.\n- Write tools validate MJML — if you get a validation error, fix the\n  source and try again.\n\nGENERATING AN EMAIL FROM SCRATCH (description + data from the user):\n1. Design the structure: hero, content/product sections, CTA, footer.\n2. Generate images with the generate_image tool (hero 1536x1024, products\n   1024x1024) and put the returned URLs into mj-image. Never invent image URLs.\n3. Save everything via set_document. Consistent palette, readable typography,\n   mj-button buttons with a clear CTA.\n\nAPPLYING FIXES FROM COMMENTS:\n1. list_open_comments → for each comment call get_section(section_id).\n2. A comment may concern the whole section or a specific element:\n   - object_id = null → the change concerns the whole section.\n   - object_id set (e.g. \"ab12cd\") → the change concerns ONLY the element with\n     class obj-<object_id> inside that section (object_label describes it).\n     Change only that element, leave the rest of the section untouched, and\n     keep its obj-<id> class.\n3. Apply the change requested by the comment via set_section (pass the whole\n   section with the fix applied).\n4. After a successful change, call resolve_comment(id) — for EVERY comment whose\n   requested change you applied, not just the last one. A comment left open is a\n   pin still sitting on the canvas asking for work that is already done.\n5. Say explicitly which comments you marked resolved.\n\nHOW TO REPLY:\n- The user is looking at the editor. Your tool calls have already changed what\n  they see, so the email does not need describing — describing it asks them to\n  read a summary of something already on screen.\n- After creating or editing an email, reply in one or two short sentences:\n  confirm what changed and invite the next change. No section list, no\n  breakdown of the design, no implementation notes.\n- If the user asks for an explanation, a summary or a rationale, give one\n  properly. This is about not volunteering it unasked.\n- Resolved comments are the exception to brevity: always name them (see above).\n  That one is not visible in the editor until the pin disappears, and a person\n  who commented needs to know it was answered.",
  "tools": [
    {
      "name": "get_document",
      "description": "Returns the document's full MJML source together with the list of its sections (section_id plus a short preview). Call this first to learn the current state.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "get_section",
      "description": "Returns the MJML source of the single section with the given section_id.",
      "input_schema": {
        "type": "object",
        "properties": {
          "section_id": {
            "type": "string",
            "description": "Identifier from the sec-<id> class."
          }
        },
        "required": [
          "section_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_document",
      "description": "Replaces the ENTIRE document with new MJML source. Use only when creating an email from scratch or rebuilding it on request; for targeted edits use set_section, insert_section or remove_section. A rewrite reassigns every sec-* id, so the comments anchored to them are deleted — on a document that already has sections this call is REFUSED unless confirm_full_rewrite is true. The document must be complete (<mjml><mj-body>...</mj-body></mjml>). Sections without a sec-* class are assigned one automatically. Rejected if the MJML does not compile.",
      "input_schema": {
        "type": "object",
        "properties": {
          "mjml": {
            "type": "string",
            "description": "Complete MJML document."
          },
          "confirm_full_rewrite": {
            "type": "boolean",
            "description": "true only when the user asked for the email to be rebuilt from scratch; false otherwise. Must be true to replace a document that already has sections, because the rewrite deletes every comment anchored to a section id it does not preserve."
          }
        },
        "required": [
          "mjml",
          "confirm_full_rewrite"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_section",
      "description": "Replaces a single section. `mjml` is exactly one <mj-section>...</mj-section> element. The section keeps its sec-<id> even if you supply a different one — that id anchors existing comments. Rejected if the resulting document does not compile.",
      "input_schema": {
        "type": "object",
        "properties": {
          "section_id": {
            "type": "string",
            "description": "Section to replace."
          },
          "mjml": {
            "type": "string",
            "description": "One complete <mj-section> element."
          }
        },
        "required": [
          "section_id",
          "mjml"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "insert_section",
      "description": "Inserts a new section (exactly one <mj-section> element) after after_section_id, or at the end of the email when it is null. Returns the id assigned to the new section.",
      "input_schema": {
        "type": "object",
        "properties": {
          "mjml": {
            "type": "string",
            "description": "One complete <mj-section> element."
          },
          "after_section_id": {
            "type": [
              "string",
              "null"
            ],
            "description": "Insert after this section, or null to append at the end of the body."
          }
        },
        "required": [
          "mjml",
          "after_section_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "remove_section",
      "description": "Removes the section with the given section_id.",
      "input_schema": {
        "type": "object",
        "properties": {
          "section_id": {
            "type": "string",
            "description": "Section to remove."
          }
        },
        "required": [
          "section_id"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "generate_image",
      "description": "Generates an image and returns a public URL to put into an mj-image src. Never invent image URLs — always obtain them from this tool.",
      "input_schema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "English, descriptive: subject, style, composition, palette."
          },
          "size": {
            "type": "string",
            "enum": [
              "1024x1024",
              "1536x1024",
              "1024x1536"
            ],
            "description": "1536x1024 for heroes, 1024x1024 for products, 1024x1536 for portraits."
          }
        },
        "required": [
          "prompt",
          "size"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "list_open_comments",
      "description": "Returns the open comments on this document: id, section_id, object_id, object_label and body. object_id null means the comment is about the whole section.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "resolve_comment",
      "description": "Marks a comment as resolved. Call only after applying the change it asked for.",
      "input_schema": {
        "type": "object",
        "properties": {
          "comment_id": {
            "type": "string",
            "description": "Comment to resolve."
          }
        },
        "required": [
          "comment_id"
        ],
        "additionalProperties": false
      }
    }
  ],
  "mutating_tools": [
    "set_document",
    "set_section",
    "insert_section",
    "remove_section",
    "resolve_comment"
  ],
  "section_id_argument": {
    "get_section": "section_id",
    "set_section": "section_id",
    "remove_section": "section_id",
    "insert_section": "after_section_id"
  }
}
