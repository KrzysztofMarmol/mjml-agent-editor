# deps
node_modules/
.venv/
__pycache__/
*.pyc

# env / secrets
.env
.env.local
.env*.local

# build
.next/
out/
dist/
.turbo/
*.tsbuildinfo
coverage/
*.egg-info/

# supabase local
supabase/.temp/
supabase/.branches/

# misc
.DS_Store
*.log

# local agent tooling (MCP + skills) — do not commit
/.mcp.json
/.agents/
/.claude/skills/
/.claude/worktrees/
/skills-lock.json

# turbo build cache
.turbo/
web/.turbo/

# Leftovers from the pre-monorepo layout. The directory survives in working copies that
# predate the move because it still holds untracked .env files and node_modules; nothing
# in it belongs to the repository any more.
/web/
/agent/
