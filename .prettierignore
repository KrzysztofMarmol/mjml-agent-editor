dist
.next

# Generated by `pnpm --filter @mjml-agent-editor/core build`; formatting it here would
# fight the emitter on every build.
packages/agent-core/contract/

# The Next.js app came from the pre-monorepo `web/` tree; formatting it wholesale now would
# bury the editor extraction in a whitespace diff. Formatted once that lands.
apps/example/

# Python: formatted and linted by ruff, not Prettier.
agent-python/
node_modules
pnpm-lock.yaml
package-lock.json
uv.lock
.turbo
supabase/.branches
supabase/.temp
