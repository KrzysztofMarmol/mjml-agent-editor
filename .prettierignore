dist
.next

# Generated by `pnpm --filter @mjml-agent-editor/core build`; formatting it here would
# fight the emitter on every build.
packages/agent-core/contract/

# Pre-monorepo code, still on its own npm/uv toolchain. Formatting it now would bury
# the extraction work in a whitespace diff — it gets formatted when `web/` becomes
# `apps/example` and `agent/` becomes `agent-python/`.
web/
agent/
node_modules
pnpm-lock.yaml
package-lock.json
uv.lock
.turbo
supabase/.branches
supabase/.temp
