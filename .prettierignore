dist
.next

# Generated by `pnpm --filter @mjml-agent-editor/core build`; formatting it here would
# fight the emitter on every build.
packages/agent-core/contract/

# Still on its own npm toolchain; formatting it now would bury the extraction work in a
# whitespace diff. Gets formatted when it becomes `apps/example`.
web/

# Python: formatted and linted by ruff, not Prettier.
agent-python/
node_modules
pnpm-lock.yaml
package-lock.json
uv.lock
.turbo
supabase/.branches
supabase/.temp
